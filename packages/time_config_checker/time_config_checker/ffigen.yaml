# Run with `dart run ffigen --config ffigen.yaml`.
name: TimeConfigCheckerBindings
description: |
  Bindings for `src/time_config_checker.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/time_config_checker_bindings_generated.dart'
headers:
  entry-points:
    - 'src/time_config_checker.h'
  include-directives:
    - 'src/time_config_checker.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
